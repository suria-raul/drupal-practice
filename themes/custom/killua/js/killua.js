/**
 * @file
 * killua behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.killua = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
